{% extends 'base.html.twig' %}

{% block title %}Bidding competition{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-sm-4"></div>
        <div class="col-sm-4">
            <h4>North's hand:</h4>

            {% for card_type in North_hand %}
                <!-- <li>{{ dump(card_type) }}</li>-->
                {% for cards in card_type %}

                    <img style="height: 12%; width: 12%" src="{{ cards.link }}" alt="" />

                {% endfor %}
                <br />
            {% endfor %}

        </div>
        <div class="col-sm-4"></div>
    </div>

    <div class="row">
        <div class="col-sm-4">
            <h4>West's hand:</h4>

            {% for card_type in West_hand %}
                <!-- <li>{{ dump(card_type) }}</li>-->
                {% for cards in card_type %}

                    <img style="height: 12%; width: 12%" src="{{ cards.link }}" alt="" />

                {% endfor %}
                <br />
            {% endfor %}
        </div>
        <div class="col-sm-4"></div>
        <div class="col-sm-4">

            <h4>East's hand:</h4>

            {% for card_type in East_hand %}
                <!-- <li>{{ dump(card_type) }}</li>-->
                {% for cards in card_type %}

                    <img style="height: 12%; width: 12%" src="{{ cards.link }}" alt="" />

                {% endfor %}
                <br />
            {% endfor %}
        </div>
    </div>


    <div class="row">
        <div class="col-sm-4"></div>
        <div class="col-sm-4">
            <h4>South's hand:</h4>

            {% for card_type in South_hand %}
                <!-- <li>{{ dump(card_type) }}</li>-->
                {% for cards in card_type %}

                    <img style="height: 12%; width: 12%" src="{{ cards.link }}" alt="" />

                {% endfor %}
                <br />
            {% endfor %}
        </div>
        <div class="col-sm-4"></div>
    </div>


    <script type="text/javascript">
        console.log("memem");
        var webSocket = WS.connect("ws://127.0.0.1:8080");

        webSocket.on("socket/connect", function(session){
            //session is an Autobahn JS WAMP session.

            console.log("Successfully Connected!");
        })

        webSocket.on("socket/disconnect", function(error){
            //error provides us with some insight into the disconnection: error.reason and error.code

            console.log("Disconnected for " + error.reason + " with code " + error.code);
        })
    </script>

{% endblock %}